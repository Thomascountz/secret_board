<h1>Post Board</h1>

<% @posts.each do |post| %>
  <ul>
    <li>
      <%= post.body %> | 
      <% if logged_in? %>
        <%= post.user.name %> 
      <% else %>
        <%= "Anonymous" %>
      <% end %>
    </li>
  </ul>
<% end %>